# Defect catalog (append-only; the reader reads this FIRST)

One line per class. `Detect` says whether a script catches it (M), a reader must judge it (J),
or a script catches most of it (M~). `Seen` counts confirmed occurrences across reads; two
confirmed occurrences of a J or M~ class means: write the check. Quotes are real findings,
scrubbed of names.

| Id | Class | The tell (a real quote) | Detect | Fix pattern | Seen |
|---|---|---|---|---|---|
| C01 | Sentence over 30 words packing a statistic, a parenthetical and a caveat | "From a 112-medium dataset counted by pattern frequency... (the share of how often mediums with that tag get asked, counted off a 112-problem dataset) at 23.8%, and sliding window is 9.9%." (46 words) | M | One idea per sentence; the parenthetical becomes its own sentence | 4 |
| C02 | Term used before, or without, its definition | "amortised" defined two sentences after use; "fact table", "shuffle join" never defined; "hash table" used on the run sheet, defined three sections later | M~ (term list) + J (new terms) | One plain clause at first use; if it needs a paragraph and is not needed, cut the term | 5 |
| C03 | Number with no origin | "501 entries" (no stream named); "156 mentions" (no dataset); "Fourteen lines." (it was fifteen) | M~ (percent, large counts, "N mentions") + J | Same sentence names the source, or the number is cut | 5 |
| C04 | Study-only tangents written as densely as the live material | Escalation answers with bucket-hash arithmetic and database analogies on a page that labels them "not for the room" | J | Three questions, three to five plain sentences each; "cannot stream, here is what breaks" is a valid answer | 2 |
| C05 | Stale plan references after a re-cut | "Day 1, problem 1 of 3"; "the plan's nine problems"; a weekday in a header | M (banned list per project) | Ban the forms; a re-cut sweeps every file, not the new ones | 3 |
| C06 | Tenure flattery or age comparison | "juniors get slack on careless mistakes and seniors do not"; "a 24-year-old grinding LeetCode cannot produce this" | M (banned phrases) | State the rule without the population; end on the answer, never on the reader | 3 |
| C07 | Difficulty framing that licenses under-preparing | "Two Sum asks little of you"; "obviously"; "simply" | M (banned words) | Describe the size of the task, not its ease | 3 |
| C08 | Sentence split across table rows, or a stray divider row | A table directly followed by prose or by `---` with no blank line renders the next line as a row | M (normalizer) | Blank line after every table block | 2 |
| C09 | Picture that decorates instead of carrying the mechanism | "Picture one person standing alone in a doorway, asked whether they are strictly ahead of themselves" for a `<` versus `<=` boundary bug: cannot re-derive where the collapse happens | J | Reader must be able to re-derive the algorithm's step from the picture alone; otherwise rewrite the picture around the actual step | 1 |
| C10 | Picture that describes a DIFFERENT bug than the code demonstrates | Picture says "comparing against the card just before instead of the open shift"; the broken line is `last[1] = cur[1]` (end assignment), and the comparison is identical to the correct code | J | Read the broken code's changed line, then write the picture of THAT | 1 |
| C11 | Markdown-speak in rendered prose | "'Beat 9' means the section headed `## 9.`" on a page where headings render as numbered badges | M (regex `## \d` in prose) | Say what the reader sees: "the block numbered 09" | 1 |
| C12 | Duplicate numbering across embedded sources | An embedded section badge "02" and a walkthrough badge "02" on one page | M (builder) | Prefix embedded units (W2, D1) | 1 |
| C13 | Task list without the physical next action | "Read W17, close it, solve cold" with no file, no command | J, then structural | Every task carries its command line, generated by the builder | 1 |
| C14 | Time chip under-costed against the unit's own header | Run sheet charges 25 min; the walkthrough's header says 36 min reading plus 25 solving | M (budget gate) | Derive the chip from the unit's header; refuse a run sheet whose chips do not sum to its total | 1 |
| C15 | Meta-note warning about content that no longer exists | A banner translating "Zach" on a page that no longer contains the word | J | After a rewrite, re-read every banner and correction note against the new page | 1 |
| C16 | Reassurance about the reader's feelings with no evidence on the page | "The dread has predicted nothing all search" | J | Cut, or give the receipt in the same sentence | 1 |
| C17 | Gate splitter artifacts counted as defects | A period inside a bold label, a semicolon legend, a bare `<=` read as an HTML tag, a period directly followed by a closing quote | M (gate maintenance) | Split on bold labels and semicolons; strip inline code before tags; the writer avoids `.`+`"` with no space | 3 |

## False positives (do not report these)

- Bold "say this out loud" lines in first person: talking out loud is the skill being taught.
- A frozen figure caption or code comment using a term: define the term BEFORE the figure in prose.
- A deliberately broken code block labelled broken: assert the bug reproduces; do not fix it.
- The honest inverse of a tenure line, marked as such ("shipped for years; never did this under a clock").

## Scoring (report both, separately, every read)

- **Taught me** 1 to 10: could the reader re-derive the mechanism closed-book from the pictures and traces.
- **Finish without disengaging** 1 to 10: bounded asks, no stalls, no rereads.
A fix pass is done when the second score has not dropped (definitions and caveats add mass).
